DATABASE_URL=postgresql://user:password@host/database?sslmode=require
# 使用 node -e "console.log(require('crypto').randomBytes(32).toString('hex'))" 產生，僅存於 apps/api/.env.local
USER_SESSION_SECRET=
# 正式環境必填，以逗號分隔完整可信前端 origin；本機開發不需要設定
USER_AUTH_ALLOWED_ORIGINS=
